{
    "builds": [
         { "src": "package.json", "use": "@vercel/static-build", "config": { "outputDirectory": "out" } }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/index.html" }
    ]
  }
